# macOS
.DS_Store

# Environment files (contains secrets)
.env
**/.env

# Keep example files
!.env.example
!**/.env.example

# Binary files folder (only track .gitkeep and README)
binary-files/*
!binary-files/.gitkeep
!binary-files/README.md

# Claude Code local settings (personal preferences)
.claude/settings.local.json
